<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logoapp"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".task_activities.PreviewEditTask"
            android:exported="false" />
        <activity
            android:name=".task_activities.EditTask"
            android:exported="false" />
        <activity
            android:name=".main.SelectActivity"
            android:exported="false" />
        <activity
            android:name=".login_signin.SignIn"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".DeleteGroup" -->
        <!-- android:theme="@style/MyActivityStyle" /> -->
        <!-- <activity -->
        <!-- android:name=".NewTask" -->
        <!-- android:theme="@style/MyActivityStyle" /> -->
        <!-- <activity -->
        <!-- android:name=".NewGroup" -->
        <!-- android:theme="@style/MyActivityStyle" /> -->
        <activity
            android:name=".login_signin.Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".group_activities.DeleteGroup"
            android:exported="false" />
        <activity
            android:name=".task_activities.NewTask"
            android:exported="false" />
        <activity
            android:name=".main.ColorCode"
            android:exported="false" />
        <activity
            android:name=".group_activities.NewGroup"
            android:exported="false" />
        <activity
            android:name=".group_activities.EditGroup"
            android:exported="false" />
        <activity
            android:name=".main.MainActivity"
            android:exported="true">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
    </application>

</manifest>